server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/repairs
    username: postgres
    password: default
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
